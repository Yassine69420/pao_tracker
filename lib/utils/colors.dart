import 'package:flutter/material.dart';

class AppColors {
  // Healthy Vibrant Primary Theme: Fresh Green
  static const primary = Color(0xFF2ECC71); // Fresh vibrant green
  static const onPrimary = Color(0xFFFFFFFF);
  static const primaryContainer = Color(
    0xFFD5F5E3,
  ); // Soft, light wellness green
  static const onPrimaryContainer = Color(0xFF145A32); // Deep natural green

  // Healthy Vibrant Secondary Theme: Aqua / Mint
  static const secondary = Color(0xFF1ABC9C); // Vibrant aqua/mint
  static const onSecondary = Color(0xFFFFFFFF);
  static const secondaryContainer = Color(0xFFCCF2E8); // Soft mint background
  static const onSecondaryContainer = Color(0xFF0E5F52); // Calm dark teal

  // Tertiary Theme: Calm Plum (soft modern accent)
  static const tertiary = Color(0xFF9B59B6);
  static const onTertiary = Color(0xFFFFFFFF);
  static const tertiaryContainer = Color(0xFFEAD1F3);
  static const onTertiaryContainer = Color(0xFF4A235A);

  // Error Colors: Rich red but slightly softer (health-friendly)
  static const error = Color(0xFFE74C3C);
  static const onError = Color(0xFFFFFFFF);
  static const errorContainer = Color(0xFFFADBD8);
  static const onErrorContainer = Color(0xFF641E16);

  // Surface Colors (smooth, wellness-friendly neutrals)
  static const surface = Color(0xFFFAFAFA);
  static const onSurface = Color(0xFF1C1C1C);
  static const surfaceVariant = Color(0xFFF2F2F2);
  static const onSurfaceVariant = Color(0xFF5F5F5F);

  // Outline (soft, minimalist)
  static const outline = Color(0xFF9E9E9E);
  static const outlineVariant = Color(0xFFE0E0E0);

  // Status Colors (more vibrant + health themed)
  static const success = Color(0xFF27AE60);
  static const successContainer = Color(0xFFD4EFDF);

  static const warning = Color(0xFFF39C12);
  static const warningContainer = Color(0xFFFDEBD0);

  static const expired = Color(0xFFC0392B);
  static const expiredContainer = Color(0xFFF5B7B1);

  // Near Expiry (brighter, happier amber)
  static const nearExpiry = Color(0xFFFFD54F);
  static const nearExpiryContainer = Color(0xFFFFF4C1);
}
